# web/shared

TypeScript types shared by server and client via relative imports (`../../shared/types`). Placeholder set in `types.ts` (`ArtifactClass`, `Layer`); later slices add `Artifact`, `Memo`, `RunStatus`, etc.
