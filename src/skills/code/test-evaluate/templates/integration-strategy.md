# IntegrationStrategy — `{{module_path}}`

> Run: `{{evaluate_run_id}}` · Source hash: `{{source_hash}}` · Generated: `{{timestamp}}`

## Boundary classification

| Tier | Count | Functions |
|---|---|---|
| Runtime | {{boundary_summary.runtime_count}} | {{boundary_summary.runtime_list}} |
| Logical | {{boundary_summary.logical_count}} | {{boundary_summary.logical_list}} |
| Internal (excluded) | {{boundary_summary.internal_count}} | {{boundary_summary.internal_list}} |

Module category: `{{module_category}}` (db | api | queue | file | cli)

## Recommended replacements (ranked)

| # | Mock target | Boundary fn | Tier | Risk | Gap | Score | Lifecycle pattern |
|---|---|---|---|---|---|---|---|
{{#each candidates_recommended}}
| {{rank}} | `{{mock_target}}` | `{{boundary_fn}}` | {{tier}} | {{risk}} | {{gap}} | {{score}} | `{{pattern}}` |
{{/each}}

### Per-candidate justification

{{#each candidates_recommended}}
#### {{rank}}. `{{mock_target}}` → `{{pattern}}`
- **Boundary function:** `{{boundary_fn}}` ({{tier}} tier)
- **Risk:** {{risk}} ({{category}})
- **Coverage gap:** {{gap}} of boundary branches covered only by mocks
- **Replacement value:** `{{tier_weight}} × {{risk_weight}} × {{gap}} = {{score}}`
- **Why this pattern:** {{pattern_rationale}}
- **Lifecycle notes:** {{lifecycle_notes}}
{{/each}}

## Considered but excluded

| Candidate | Reason |
|---|---|
{{#each candidates_excluded}}
| `{{candidate}}` | {{exclusion_reason}} |
{{/each}}

Exclusion reasons drawn from `references/real-integration-checklist.md`: `low-marginal-gain`, `low-failure-mode-risk`, `cost-exceeds-budget`, `over_mocking_warning`, `wrapper-coupling`, `already-covered`, `unsupported-category`.

## Over-mocking warnings

{{#if over_mocking_warnings}}
The following mocks target internal (non-boundary) functions. They should be removed from tests rather than converted to real integration — they exercise wiring not behavior.

{{#each over_mocking_warnings}}
- `{{mock_target}}` in `{{test_file}}` — resolves to internal function `{{internal_fn}}`.
{{/each}}
{{else}}
None.
{{/if}}

## Out-of-scope

- Third-party libraries classified as pure-compute (per `external-lib-policy.md`): {{out_of_scope_libs}}
- Modules already using real-integration tests (per CoverageState): {{already_real_modules}}

## Hand-off to test-integrate

Apply the recommended replacements above in score order (highest first). Use the assigned lifecycle pattern verbatim — do not improvise. After conversion, re-run `coverage_analyzer --boundary-only` to confirm `current_mock_coverage_gap` decreased.

---
*Generated by `/test-evaluate` · Soft-gate review label: `integration-strategy-review`*
