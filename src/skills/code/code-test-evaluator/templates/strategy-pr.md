# IntegrationStrategy bundle — run `{{evaluate_run_id}}`

> **Soft gate.** This PR is informational. The engine has completed evaluation and proceeds without waiting for reviewer approval. Feedback here will be applied on the next `/code-test-evaluator` run.

## Summary

| Metric | Value |
|---|---|
| Modules processed | {{stats.processed}} |
| Modules with strategies | {{stats.classified}} |
| Skipped — internal-only | {{stats.skipped_internal}} |
| Skipped — already real | {{stats.skipped_already_real}} |
| Total recommended replacements | {{stats.total_recommended}} |
| Total excluded candidates | {{stats.total_excluded}} |
| Over-mocking warnings | {{stats.over_mocking_warnings}} |

## Top 5 highest-value replacements (across all modules)

| # | Module | Mock target | Score | Pattern |
|---|---|---|---|---|
{{#each top_five}}
| {{rank}} | `{{module}}` | `{{mock_target}}` | {{score}} | `{{pattern}}` |
{{/each}}

## Per-module strategies

{{#each modules}}
### `{{module_path}}`
- Category: `{{category}}`
- Recommended: {{recommended_count}} · Excluded: {{excluded_count}} · Warnings: {{warning_count}}
- Strategy doc: [`{{strategy_path}}`]({{strategy_path}})
{{/each}}

## How to review (async)

This bundle is for **classification feedback**, not approval:
- Disagree with a boundary classification? Comment on the strategy doc with the expected tier and reasoning.
- Disagree with a lifecycle pattern? Comment with the alternative and why this candidate doesn't fit the default rule.
- Spot a missed over-mocking warning? Add it as a comment — will be picked up next run.

Engine does **not** block on this PR. The next `/code-test-evaluator` run will incorporate accepted feedback via the source-hash-delta mechanism.

## Out-of-scope for this PR

- Test code changes — `/code-test-integrator` will apply the recommendations.
- Mock removal — handled by `/code-test-integrator` once strategies are accepted.
- Quality issues with existing mocks — `/code-test-linter` and `/code-test-auditor` cover those.

## Configuration

- `--top-n`: `{{config.top_n}}`
- `--rerun-mode`: `{{config.rerun_mode}}`
- Coverage state source: `{{config.coverage_state_path}}`

---
*Label: `integration-strategy-review` · Generated by `/code-test-evaluator` · Soft gate — no block*
