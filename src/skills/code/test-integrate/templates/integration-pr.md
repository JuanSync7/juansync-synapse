# Real-Integration Test: `{{module_path}}` → `{{boundary_function}}`

> **HARD GATE** — engine is paused on this PR. Approve or reject explicitly. The engine will not advance to the next strategy item until a review signal is registered. **Never auto-merged.**

## What changed

Promoted mock-integration coverage to real-service integration for **`{{module_path}}`**, exercising the boundary call **`{{boundary_function}}`** (category: **{{category}}**, replacement_value: **{{replacement_value}}**).

- New test: `{{new_test_path}}`
- Original mock test: `{{prior_mock_test_path}}` (kept in place; will be deleted on merge)
- Lifecycle pattern: **`{{lifecycle_pattern}}`**

## Why this lifecycle pattern

{{pattern_rationale}}

Pattern decision rules applied (see `references/lifecycle-patterns.md`):
- {{rule_citation}}

## Flakiness summary

| Metric | Value |
|---|---|
| Reruns | {{rerun_count}} |
| Failures | {{failure_count}} |
| Fail rate | **{{fail_rate_pct}}%** |
| Threshold | < 2.0% |
| Status | {{flake_status}} |

Raw outcomes:
```
{{flake_outcome_log}}
```

## Service spin-up attestation

- Pattern: `{{lifecycle_pattern}}`
- Endpoint: `{{endpoint_or_image}}` (non-production)
- Ephemeral: teardown verified
- Cassette (if vcrpy): `{{cassette_path}}` — secret-scan **passed** ({{secret_scan_result}})

## Boundary call sites exercised

{{call_sites_table}}

## Test diff

<details>
<summary>Click to expand</summary>

```diff
{{test_diff}}
```

</details>

## Pre-merge checklist (reviewer)

- [ ] Lifecycle pattern matches dependency category (DB -> rollback / DDL -> schema-per-test / API -> vcrpy / queue -> ephemeral-container)
- [ ] No mock left active in the new test (AST-scan: `{{ast_scan_result}}`)
- [ ] Cassette (if any) is scrubbed of secrets (scanner: `{{secret_scan_result}}`)
- [ ] Endpoint is **not** a production host
- [ ] Flakiness fail rate < 2% over >=10 reruns
- [ ] Test exercises real service behavior (not just mocks dressed up)
- [ ] Teardown is correctly scoped (no leaked containers, no leaked DB state)

## Approval semantics

- **Approve** -> engine merges this PR, deletes the original mock test, updates `COVERAGE_STATE.yaml` + `EDGE_COVERAGE.yaml`, advances to the next strategy item.
- **Request changes with `decline` keyword** OR **close without merging** -> item marked `human-rejected`, engine advances to next item without merging this test.
- **No action** -> engine stays paused on this item; the next item is NOT processed.

## Engine metadata

- Skill: `test-integrate`
- Run id: `{{integrate_run_id}}`
- Strategy item: `{{strategy_item_id}}`
- Source `IntegrationStrategy`: `{{strategy_path}}`

---

Generated by `test-integrate`. Do not auto-approve via bot — this is the only HITL gate in the test-coverage engine.
