## code-test-generator batch {{generation_id}} — intent review

<!-- generation_id example: tg-2026-04-30-a3f7 -->

---

### Hard gate — engine stopped, nothing committed

This PR is a **HARD HITL gate**. The code-test-generator engine has halted after completing
generation and pre-gate validation. **No test files have been written to the repository.**
The engine will not proceed until a reviewer explicitly signals approval (see instructions
below). There is no timeout and no auto-approval path — silence is not consent.

Review the plain-English intent list below. You are approving or rejecting the *intent*
of the tests, not their implementation. Code diffs are not shown here by design.

---

### Batch summary

| Gap ID | Target | Layer | Tests | Status |
|--------|--------|-------|-------|--------|
{{#each gap}}
| {{gap_id}} | `{{target}}` | {{layer}} | {{test_count}} | {{status}} |
{{/each}}

<!-- Status values:
  ready               — green run + mutants killed + score met
  unresolvable        — engine could not generate a valid test
  mutation-survived   — at least one mutant survived; see Out-of-scope
  score-deficit       — assertion quality score below threshold
  implementation-coupled — tests bound to internal details, not contract
-->

---

### Descriptive intent

<!-- This is the primary review surface. Each line is derived from the @scenario and
     @asserts tags of the generated test docstrings — not from code. -->

{{#each gap}}
#### {{gap_id}} — `{{target}}`

{{#each test}}
- `{{function}}` — {{scenario}} → {{asserts}}
{{/each}}

{{/each}}

---

### Hypothesis ratio

Property tests: **{{property_count}} / {{total_count}} ({{percent}}%)** — target ≥ 30%

{{#if below_target}}
> Note: ratio is below the 30% target. Consider whether any example-based tests in this
> batch have natural property analogues. Informational only — not a blocking condition.
{{/if}}

---

### Pre-gate validation (confirmed before this PR was opened)

- Green run: all generated tests pass against the current implementation.
- Mutation threshold: ≥ {{mutation_threshold}}% of injected mutants killed.
- Assertion-quality score: each test meets or exceeds the minimum score ({{min_score}}).
- Docstring parse: all `@scenario` / `@asserts` tags parsed without error.

---

### Benchmark candidates

<!-- Flagged by benchmark_selector as "worth a dedicated benchmark". These are surfaced
     for human authoring — this skill does not auto-write benchmarks. -->

{{#if benchmark_candidates}}
The following targets were flagged as performance-sensitive. Consider adding benchmarks
before the next release cycle:

{{#each benchmark_candidates}}
- `{{target}}` — {{reason}}
{{/each}}
{{else}}
No benchmark candidates flagged in this batch.
{{/if}}

---

### Out of scope — not committed, not auto-retried

The following gaps were logged but excluded from this batch. They require human
intervention before the engine will attempt them again.

{{#each out_of_scope}}
- **{{gap_id}}** (`{{target}}`): {{status}} — {{reason}}
{{/each}}

<!-- Example reasons:
  unresolvable        — "No stable entry point found; target is a private closure."
  mutation-survived   — "Mutant on line 42 (off-by-one) survived all generated tests."
  score-deficit       — "Assertion score 0.41; threshold is 0.60."
  implementation-coupled — "Tests reference _internal_cache directly."
-->

---

### Reviewer instructions

**To approve all gaps marked `ready`:**
Comment `/approve` on this PR **or** apply the label `intent-approved`.
The engine will commit only the `ready` tests and close this PR.

**To reject the entire batch:**
Comment `/reject <reason>` **or** apply the label `intent-rejected`.
All gaps in this batch are marked `human-rejected`. No files are committed.
The engine halts until restarted with a new generation run.

**To request changes on specific gaps:**
Comment with gap IDs and notes, for example:

```
gap-007: the "empty input" scenario should also assert the error message, not just the type.
gap-012: reject — this intent describes an internal retry loop, not a public contract.
```

The engine will re-enter from STEP 1 for those gaps, carrying your notes as constraints.
Unaffected `ready` gaps remain staged and will be committed once the revised gaps pass.

---

> 🤖 Generated by /code-test-generator — `generation_id={{generation_id}}`
> Coverage state: [`COVERAGE_STATE.yaml`]({{coverage_state_url}})
